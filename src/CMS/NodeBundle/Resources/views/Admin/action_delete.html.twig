{% if not object.isRoot %}
<a href="{{ path('lyra_admin_node_delete', { 'id' : object.id }) }}" class="button action-delete {{ action.buttonIcon ? ' ui-icon-'~action.buttonIcon : '' }}{{ action.hasDialog ? ' dialog' : ''}}{{ action.buttonStyle is defined ? ' '~action.buttonStyle  : '' }}">{{ 'list.delete'|trans({}, 'LyraContentBundle') }}</a>
{% endif %}
